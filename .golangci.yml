version: "2"
run:
  timeout: 60s

formatters:
  enable:
    - gofumpt
  settings:
    gofumpt:
      module-path: collab-notes

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - revive
    